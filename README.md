# EcoLife (Expo + TypeScript)

## Запуск
1) Установи зависимости:
```bash
npm install
```

2) Запусти:
```bash
npx expo start -c
```

3) Открой в Expo Go (Android) и отсканируй QR (режим LAN).

## Структура
- `App.tsx` — вход
- `src/navigation/BottomTabs.tsx` — табы
- `src/screens/*` — экраны
